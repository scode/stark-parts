{
  "private": true,
  "packageManager": "npm@11.17.0",
  "scripts": {
    "build": "turbo run build:app && npm run assemble:catalog",
    "build:app": "bash scripts/build-app.sh",
    "assemble:catalog": "node scripts/assemble-catalog.mjs",
    "dev": "NO_COLOR=true trunk serve --release --port 1420",
    "vercel:build": "npm run build"
  },
  "devDependencies": {
    "@playwright/test": "1.60.0",
    "http-server": "14.1.1",
    "turbo": "2.10.4"
  }
}
